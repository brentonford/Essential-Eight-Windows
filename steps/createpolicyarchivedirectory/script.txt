$archivePath = "{policyExportPath}"

if (Test-Path -Path $archivePath) {
    Write-Output "Policy archive directory already exists: $archivePath"
    exit 0
}

New-Item -ItemType Directory -Path $archivePath -Force | Out-Null
Write-Output "Created policy archive directory: $archivePath"