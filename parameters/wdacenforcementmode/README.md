Set to `Audit` to deploy in audit mode (logs violations, does not block) or `Enforce` to actively block unauthorised execution. 

Always run Audit mode first and review Event Viewer before switching to Enforce.